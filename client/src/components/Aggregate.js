import React from "react";

export default class Aggregate extends React.Component {

    constructor(props) {
        super(props);
    };

    render() {
        return (
            <form action=""></form>
        )
    }
}